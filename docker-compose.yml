services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: soil
      POSTGRES_PASSWORD: soil
      POSTGRES_DB: soil_crop_advisor
    ports:
      - "5432:5432"
    volumes:
      - soil_pg_data:/var/lib/postgresql/data

volumes:
  soil_pg_data:
